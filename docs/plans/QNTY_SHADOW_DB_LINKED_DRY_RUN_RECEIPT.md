# QNTY Shadow DB-Linked Dry Run Receipt

## 1. Purpose

Record the shadow-only DB-linked funding source snapshot dry run performed against the shadow/null lane, and record the read-only prod observation that a prod sidecar/batch already existed from the timer before/during this session.

## 2. Scope

This receipt covers a single shadow writer invocation against the shadow/null-lane SQLite ledger DB at `/srv/qnty/output/paper_pnl_null_shadow_v0/paper_ledger.db`, plus the pre/post checks and safety checks taken around that invocation.

It does not cover any prod writer execution, any prod DB mutation, any `forward_obs` mutation, data refresh, migrations, schema-ensure helpers, timer/systemd changes, dependency installs, or a read-only prod DB-linked snapshot acceptance audit. Those are explicitly out of scope for this receipt.

## 3. Shadow dry run command

Run exactly once, against the shadow/null-lane DB only:

```bash
QNTY_PAPER_OUTPUT_DIR=/srv/qnty/output/paper_pnl_null_shadow_v0 \
/srv/qnty/venv/bin/python scripts/qnty-paper-sqlite-accounting.py \
  --db-path /srv/qnty/output/paper_pnl_null_shadow_v0/paper_ledger.db \
  --forward-obs-dir /srv/qnty/output/forward_obs_v1 \
  --data-dir /srv/qnty/repo/data \
  --json
```

VM repo state at run time: `/srv/qnty/repo`, `main` clean at `d4b9e94` or newer.

## 4. Shadow pre-state

- `ledger_batches` row count: 15.
- Latest rowid (15) already had a committed DB-linked snapshot from an earlier run today at 14:30 UTC.
- Watermark: `2026-07-03T00:00:00`.
- Shared `forward_obs_v1` latest observed bar: `2026-07-03T08:00:00`.
- Exactly one eligible new bar.

## 5. Shadow backup

Taken before the writer ran:

```txt
/srv/qnty/output/paper_pnl_null_shadow_v0/paper_ledger.db.before_shadow_dry_run.20260703T185642Z.bak
```

## 6. Shadow writer result

- Status code: 0.
- Output: `Committed batch 16: 1 bars, 7 events`.

## 7. Shadow DB-linked snapshot postcheck

- `ledger_batches` row count: 15 -> 16.
- Batch 16 `write_state=committed`.
- `schema_version=FUNDING_SOURCE_SNAPSHOT_SCHEMA_V1`.
- `new_watermark_bar_ts=2026-07-03T08:00:00`.
- DB file SHA `a920b027...` matched sidecar file bytes exactly.
- `PRAGMA integrity_check`: ok.

## 8. Sidecar status

- Shadow now has 2 sidecars.
- New sidecar: `07108989...json`, 7344 bytes.
- Both sidecars are under the expected directory: `/srv/qnty/output/paper_pnl_null_shadow_v0/funding_source_snapshots/`.

## 9. forward_obs safety check

`forward_obs_v1` sha256 tree fingerprint identical before/after this shadow run. No mutation caused by this task.

## 10. Prod safety check

Prod DB sha256 identical before/after this shadow run: `5274a1cf...`. No prod writer was run by this task. No prod DB mutation was caused by this task.

## 11. Prod timer observation

- Prod DB row count: 39.
- One prod sidecar exists under `/srv/qnty/output/paper_pnl_v1/funding_source_snapshots/`.
- Prod sidecar mtime was 16:21:40 UTC, before this shadow session started at 18:56 UTC.
- This sidecar was likely generated by the prod paper-pnl timer after the VM code/schema rollout, not by this task.
- This was not caused by the shadow run in this session.
- Current/prod clean-carry status was not verified in this task.

## 12. What was not done

- No prod writer was run.
- No shadow writer was run more than once.
- No prod DB mutation.
- No shadow DB mutation beyond the single documented dry run write.
- No `forward_obs` mutation.
- No data refresh.
- No migrations.
- No schema-ensure helpers.
- No timer/systemd changes.
- No dependency installs.
- No read-only prod DB-linked snapshot acceptance audit (that is the next safe step, not this receipt).

## 13. Interpretation

```txt
This receipt proves the shadow/null lane can produce a committed DB-linked snapshot sidecar and matching DB reference for a new batch.

This is not an edge claim.
This is not a profitability claim.
EDGE_UNPROVEN remains preserved.

Prod/current evidence remains CAVEATED_ENGINE_SEMANTICS until a separate read-only prod DB-linked snapshot acceptance audit verifies the prod timer-created batch.

Prod/current ledgers are not relabeled as CLEAN_NET_OF_CARRY by this receipt.
```

## 14. Next safe step

```txt
READ_ONLY_PROD_DB_LINKED_SNAPSHOT_ACCEPTANCE_AUDIT
```

This next step is read-only because prod already has a timer-created sidecar/batch (row count 39, sidecar mtime 16:21:40 UTC, prior to this shadow session). The audit should inspect and verify that existing prod batch and sidecar — checking DB-linked snapshot reference fields, sidecar file-byte hash, envelope payload digest, schema/bundle identity, source coverage, arithmetic, and funding re-sum — without running any writer, without mutating the prod DB, and without mutating `forward_obs`.

## 15. Verdict

`SHADOW_DB_LINKED_SNAPSHOT_DRY_RUN_COMPLETE`
